# agents work-in-progress (not ready for sync)
.agents/wip-skills/

# tests live under .tests/ (dotfiles ignored by chezmoi)

# README stays with git only
README

{{ if ne .chezmoi.os "darwin" -}}
Library/
{{ end -}}
